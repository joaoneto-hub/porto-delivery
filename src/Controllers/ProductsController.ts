import { FastifyReply, FastifyRequest } from 'fastify';

class Productsontroller {

  // Lista todos os produtos
  static async getAll(req: FastifyRequest, reply: FastifyReply): Promise<void> {
    
  }
}